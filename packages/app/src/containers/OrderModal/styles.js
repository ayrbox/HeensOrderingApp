const styles = theme => ({
  modalContainer: {
    height: '100%',
    width: '250px',
    padding: `${theme.spacing.unit * 4}px`,
  },
  orderButton: {
    marginBottom: `${theme.spacing.unit}px`,
  },
  fullWidth: {
    width: '100%',
  },
});

export default styles;
